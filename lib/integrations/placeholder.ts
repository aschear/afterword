/**
 * Placeholder for future metadata APIs (e.g. Open Library, Google Books)
 * and normalization of extracted book titles.
 */
export async function normalizeBookTitle(_rawTitle: string): Promise<{ title: string; author?: string; isbn?: string } | null> {
  return null;
}
